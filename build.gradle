repositories {
    mavenCentral()
}

allprojects {
    apply plugin: "base"
    group 'com.vasan'
    version '1.0.0-SNAPSHOT'
}

subprojects {
    apply plugin: "java"

    sourceCompatibility = "1.8"

    repositories {
        mavenCentral()
    }
}
